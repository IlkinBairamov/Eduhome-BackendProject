@model List<Course>
@{

    ViewData["Title"] = "Index";
    var controller = ViewContext.RouteData.Values["controller"].ToString();
}


<!-- Banner Area Start -->
@await Component.InvokeAsync("Bunner");
<!-- Banner Area End -->
<!-- Course Start -->
<div class="course-area pt-150 pb-150">
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <div class="course-title">
                    <h3>search courses</h3>
                </div>
                <div class="course-form">
                    <form id="search" action="#">
                        <input id="input-search" type="search" placeholder="Search..." name="search" />
                        <input type="hidden" name="name" value="@controller" id="controller" />
                        <button type="submit">search</button>
                    </form>
                </div>
            </div>
        </div>
        <div class="row" id="old">
            @foreach (var item in Model)
            {
                <div class="col-md-4 col-sm-6 col-xs-12">
                    <div class="single-course mb-70">
                        <div class="course-img">
                            <a asp-action="Detail">
                                <img src="~/img/course/@item.Image" alt="course">
                                <div class="course-hover">
                                    <i class="fa fa-link"></i>
                                </div>
                            </a>
                        </div>
                        <div class="course-content">
                            <h3><a asp-action="Detail">@item.Name</a></h3>
                            <div class="mb-6" style="height:72px ; overflow:hidden">
                                <p>@item.Text</p>
                            </div>

                            <a class="default-btn" asp-controller="Course" asp-action="Detail">read more</a>
                        </div>
                    </div>
                </div>
            }
        </div>
        <div class="row" id="new">

        </div>
    </div>
</div>
<!-- Course End -->
<!-- Subscribe Start -->
@await Component.InvokeAsync("Subscribe");
<!-- Subscribe End -->
